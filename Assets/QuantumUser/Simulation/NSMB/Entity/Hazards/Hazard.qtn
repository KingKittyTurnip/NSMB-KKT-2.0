component Hazard {
	[ExcludeFromPrototype] bool IsHazard;

	bool IsHefty;
	bool IPWSUntilGround;
	byte IPWSTime;
	FPVector2 SpawningVelocityRange;

	[ExcludeFromPrototype] byte Team;
	[ExcludeFromPrototype] byte LifeTime;
	[ExcludeFromPrototype] bool Inactive;
}

enum SpawnReason : Byte {
	Normal,
	Bulb,
	Fridge,
}

signal InitializeHazard(EntityRef thisEntity, EntityRef owner, FPVector2 spawnpoint, SpawnReason spawnReason);