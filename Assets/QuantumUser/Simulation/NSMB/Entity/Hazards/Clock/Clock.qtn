component Clock {
	int Time;
    ClockType type;
}

event ClockCollect {
	EntityRef Entity;
	FPVector2 pos;
	int Time;
    ClockType type;
	bool Overtime;
}

enum ClockType : Byte {
	Add,
	Subtract,
	Timeup,
	Reset,
}